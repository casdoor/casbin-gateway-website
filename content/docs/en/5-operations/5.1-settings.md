---
title: "5.1 Settings and configuration"
description: "Editing the file after that first start does nothing — except for the keys read before the database is open:"
---

**Everything is optional.** Settings are changed on the **Settings** page and stored in the database, so nothing has to be edited by hand and nothing has to be restarted.

## The file and the page

`conf/app.conf`, next to the executable, **seeds** the settings on the very first start and explains each one. The one-step install has no file beside it and seeds from the copy baked into the binary instead.

Editing the file after that first start does nothing — except for the keys read before the database is open:

`httpport`, `driverName`, `dataSourceName`, `dbName`, `redisEndpoint`.

## The settings people actually change

| Setting | Default | What it does |
| --- | --- | --- |
| `httpport` | `17000` | Port of the web UI and the REST API |
| `httpaddr` | `127.0.0.1` | Interface the web UI binds to — [5.2](./5.2-security-and-network.md) |
| `driverName` / `dataSourceName` | `sqlite` / `./data/casbin-gateway.db` | Where data is stored |
| `llmRecordMode` | `full` | How much of each relayed request is kept — [4.2](../4-governance/4.2-llm-records.md) |
| `providerProbeMode` | `auto` | Whether providers are probed on their own, only when asked (`manual`), or never (`off`) — [3.4](../3-providers/3.4-authenticity.md) |
| `apiKeyEncryptionKey` | empty | Encrypts provider API keys at rest (AES-256-GCM) |
| `casdoorEndpoint` | empty | Switches sign-in over to [Casdoor](https://casdoor.org) SSO |

## The Settings page, section by section

| Section | Holds |
| --- | --- |
| **Startup** | Whether Gateway starts when you log in. The tray menu's **Start at Login** is the same switch |
| **LLM records** | The recording mode and the limits around it — [4.2](../4-governance/4.2-llm-records.md) |
| **Channel probes** | Probe mode and how long before a report goes stale — [3.4](../3-providers/3.4-authenticity.md) |
| **Agents** | Agent state directory, how many agent records to keep, how often to poll a monitored agent |
| **Sign-in** | The five Casdoor fields; empty means Gateway's own user table |
| **Security** | `apiKeyEncryptionKey` and the relay token — [5.2](./5.2-security-and-network.md) |
| **Network** | Outbound SOCKS5 proxy, allowed hosts, allowed origins |
| **Backups** | Snapshots of the configuration — [5.3](./5.3-backups.md) |
| **Cloud sync** | A copy of every backup, somewhere that is not this machine — [5.3](./5.3-backups.md) |
| **Import and export** | The configuration as one file, to move to another machine and read back |

## Using MySQL instead of SQLite

XORM is used, so every database it supports works. Point Gateway at your server and it creates `dbName` on first start if it does not exist:

```ini
driverName = mysql
dataSourceName = root:123@tcp(localhost:3306)/
dbName = casbin_gateway
```

## Connecting Casdoor

[Casdoor](https://casdoor.org) is optional and takes over member management. Create an organization and an application for Gateway in a Casdoor instance, then fill in the five fields of **Settings → Sign-in**.

Sign-in redirects to Casdoor as soon as `casdoorEndpoint` is set, which also enables [OAuth logins](https://casdoor.org/docs/provider/oauth/overview).

## Checking the result instead of the file

The startup summary reports what Gateway is actually doing — which port, which database, which settings source, whether the relay needs a token ([1.2](../1-getting-started/1.2-installation.md)).

Next: [5.2 Security and serving other machines](./5.2-security-and-network.md).
